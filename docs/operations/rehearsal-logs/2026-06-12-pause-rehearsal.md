# Pause-rehearsal log — 2026-06-12

> Generated by `pnpm devnet:pause-rehearsal`. Closes #231 partial.
> Verification table + notes filled in by operator.

## Result

✓ **Drill completed successfully.** Pause → manual gate verification → unpause cycle completed end-to-end on devnet.

## Environment

- **RPC:** https://api.devnet.solana.com
- **Authority:** `64XM177Vm6zirzQnjU1juQ9TLqDsZVsCcZzfgEgVCffm` (deployer)
- **ProtocolConfig PDA:** `3c9MmoM8ZGQGCrKMFGvJcCtvD78jEPa2JZtLwTvJoTMV`

## Transactions

- **Pause:** [`5SDa6vjVEcVPg6LxsfjtuqgKq83sjEpqdhjEMo5G5iQP7e3boV4nad1wMvb6gfySPnQnbh1UZX8yndPzuGfYw7BK`](https://solscan.io/tx/5SDa6vjVEcVPg6LxsfjtuqgKq83sjEpqdhjEMo5G5iQP7e3boV4nad1wMvb6gfySPnQnbh1UZX8yndPzuGfYw7BK?cluster=devnet)
- **Unpause:** [`4BSWKh5HhPwi9tGikyny4wyA1Pk2ue1hFqN42jNEqbukXcdVCdsASRE8CnfkZfxPiVFyiT4qLzw9XGftAQGWofJu`](https://solscan.io/tx/4BSWKh5HhPwi9tGikyny4wyA1Pk2ue1hFqN42jNEqbukXcdVCdsASRE8CnfkZfxPiVFyiT4qLzw9XGftAQGWofJu?cluster=devnet)

## Manual verification

Mark each row as ✓ (gate fired with ProtocolPaused), ✗ (no error or wrong error), or — (not attempted):

| Instruction             | Expected           | Observed                                                 |
| ----------------------- | ------------------ | -------------------------------------------------------- |
| `create_pool`           | ProtocolPaused     | ✓ — `ProtocolPaused` (6024 / 0x1788) on seed 101 attempt |
| `join_pool`             | ProtocolPaused     | — not attempted                                          |
| `contribute`            | ProtocolPaused     | — not attempted                                          |
| `claim_payout`          | ProtocolPaused     | — not attempted                                          |
| `release_escrow`        | ProtocolPaused     | — not attempted                                          |
| `deposit_idle_to_yield` | ProtocolPaused     | — not attempted                                          |
| `harvest_yield`         | ProtocolPaused     | — not attempted                                          |
| `escape_valve_list`     | ProtocolPaused     | — not attempted                                          |
| `escape_valve_buy`      | ProtocolPaused     | — not attempted                                          |
| `settle_default`        | NOT ProtocolPaused | — not attempted                                          |

`create_pool` was exercised as the representative gated instruction — it
shares the identical `constraint = !config.paused @ ProtocolPaused`
account guard as the other 8 user-facing gated ixs (verified by grep:
all 13 gated ixs use the same `config` account constraint, so the gate
fires at account-validation time before any handler logic). The
ungated `settle_default` carve-out was not exercised this run.

## Notes

- **Wallet-resolution fix uncovered during this drill.** The rehearsal
  first reverted with `Unauthorized (6023)` because it resolved its
  signer as `SOLANA_WALLET ?? ~/.config/solana/id.json`, which on this
  machine is a non-authority key. `pause` is gated on
  `authority.key() == config.authority` (the deployer). Fixed to fall
  back through `ANCHOR_WALLET` then `keypairs/deployer.json` (the actual
  devnet deployer location). The successful run above used
  `SOLANA_WALLET=$PWD/keypairs/deployer.json` explicitly.
- **Added `pnpm devnet:unpause`** — an idempotent safety net for the
  case where the rehearsal exits after pausing but before its unpause
  step, leaving devnet frozen. Reads the flag, no-ops if already false,
  else sends `pause(false)` and verifies.
- No anomalies on the exercised gate. Drill closed cleanly with
  `paused = false` verified on-chain.
