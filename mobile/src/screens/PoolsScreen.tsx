import { PlaceholderScreen } from "./PlaceholderScreen";

export function PoolsScreen() {
  return (
    <PlaceholderScreen
      title="Pools"
      blurb="List + detail of ROSCA pools the member belongs to. Wires to the indexer's reads in Fase 2."
    />
  );
}
