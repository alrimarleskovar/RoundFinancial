import { PlaceholderScreen } from "./PlaceholderScreen";

export function WalletScreen() {
  return (
    <PlaceholderScreen
      title="Wallet"
      blurb="Phantom mobile (Fase 2) and Seed Vault Wallet (pre-Canary, Seeker testers). For now: address display only, no signing."
    />
  );
}
