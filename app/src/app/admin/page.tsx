import { AdminClient } from "@/components/admin/AdminClient";

// /admin · Demo Studio.
// Parallel dashboard for video recording — fully isolated from the
// production /home flow (own state, own layout, no DeskShell).

export default function AdminPage() {
  return <AdminClient />;
}
