{
  "name": "@roundfi/orchestrator",
  "version": "0.1.0",
  "private": true,
  "description": "RoundFi lifecycle orchestrator — deterministic, demo-first driver for pool creation, cycles, defaults, and close.",
  "license": "Apache-2.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./events": "./src/events.ts",
    "./logger": "./src/logger.ts",
    "./setup": "./src/setup.ts",
    "./runCycle": "./src/runCycle.ts",
    "./simulateDefault": "./src/simulateDefault.ts",
    "./lifecycleDemo": "./src/lifecycleDemo.ts",
    "./indexer": "./src/indexer.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@roundfi/sdk": "workspace:*",
    "@coral-xyz/anchor": "^0.30.1",
    "@solana/spl-token": "^0.4.8",
    "@solana/web3.js": "^1.95.3"
  },
  "devDependencies": {
    "typescript": "^5.6.2"
  }
}
